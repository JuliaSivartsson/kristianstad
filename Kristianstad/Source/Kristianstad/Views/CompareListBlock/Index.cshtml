@using EPiServer.Framework.Web.Resources
@using EPiServer.Editor

@if (Model.OrganisationalUnits.Count > 0)
{
    <nav class="section-menu">
        <h4>Att jämföra</h4>
        <ul role="navigation">
            <ul role="navigation">
                @foreach (var ou in @Model.OrganisationalUnits)
                {
                    <li>
                        <div class="row">
                            <div class="column small-12 medium-12 large-8 content">
                                <a href="@ou.URL">@ou.Name</a>
                            </div>
                            <div class="column content-page right-content small-12 medium-12 large-4">
                                <a href="@String.Format("{0}AddOuToCompare/?id={1}&redirectBackTo={2}", ou.URL, ou.ID, Model.CurrentLink)">Ta bort</a>
                            </div>
                        </div>
                    </li>
                }
            </ul>
        </ul>
    </nav>
    if (Model.CompareLink != null)
    {
        if (Model.OrganisationalUnits.Count > 1)
        {
            <a href="@Model.CompareLink" class="button">Jämför objekt</a>
        }
        else
        {
            <span class="button secondary" title="Välj minst två objekt för att jämföra.">Jämför objekt</span>
        }

        <a href="@String.Format("{0}ClearList?id={1}&redirectBackTo={2}", Model.ClearLink, Model.CategoryId, Model.CurrentLink)" class="button">Rensa</a>
    }
}