@using Kristianstad.ViewModels.Compare

@model GroupCategoryListModel

@for(int i = 0; i < Model.GroupCategories.Count; i++)
{
    @Html.Action("Preview", "GroupCategoryPage", new { currentPage = Model.GroupCategories[i], groupCategoryListModel = Model })
    <hr />
}
